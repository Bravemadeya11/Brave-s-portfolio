{
  "name": "brave-portfolio-server",
  "version": "1.0.0",
  "description": "Small Express server to forward contact form messages via SMTP",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "rm -rf dist && mkdir -p dist && cp portfolio_website.html dist/index.html && cp -r assets dist/assets"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "nodemailer": "^6.9.1"
  }
}
